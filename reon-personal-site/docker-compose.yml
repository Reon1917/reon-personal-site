services:
  app:
    build: .
    image: lin-portfolio-app
    container_name: lin-portfolio-app
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
